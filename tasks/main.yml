# SPDX-FileCopyrightText: 2023 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 Bergrübe
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Perform Stirling PDF installation tasks
  when: stirling_pdf_enabled | bool
  tags:
    - setup-all
    - setup-stirling-pdf
    - install-all
    - install-stirling-pdf
  block:
    - name: Validate Stirling PDF configuration
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/validate_config.yml"
    - name: Install Stirling PDF
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/install.yml"

- name: Perform Stirling PDF uninstallation tasks
  when: not stirling_pdf_enabled | bool
  tags:
    - setup-all
    - setup-stirling-pdf
  block:
    - name: Uninstall Stirling PDF
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/uninstall.yml"
